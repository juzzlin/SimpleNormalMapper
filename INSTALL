Build & install instructions
============================

These are build & install instructions for Simple Normal Mapper.

Build dependencies
------------------

Currently the build depends on Qt5 only.

Building
--------

The simplest way to build Simple Normal Mapper is to import the project into Qt Creator.

Building from command line on Linux:

$ ./configure
$ make

-- Jussi Lind <jussi.lind@iki.fi>
